# /etc/cron.d/attrax-backup-remote
# 每日异地备份由 offsite-backup.timer 管理，使用受限 SSH 账号和 Borg 加密。
# 本文件不保留旧 backup-remote.sh 任务，避免绕过加密与恢复验证。
# 主机恢复时先安装并验证 offsite-backup.timer，再安装本文件：
#   install -o root -g root -m 644 docs/infra/cron-attrax-backup-remote /etc/cron.d/attrax-backup-remote
