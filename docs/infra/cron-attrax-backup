# /etc/cron.d/attrax-backup
# 每日 03:40 由 offsite-backup.timer 调用 /usr/local/sbin/attrax-backup，
# 完成本地完整归档、Borg 加密异地传输与恢复校验。
# 该文件保留为空调度说明，避免重新安装文档片段时恢复重复 cron。
# 主机恢复时先安装并验证 offsite-backup.timer，再安装本文件：
#   install -o root -g root -m 644 docs/infra/cron-attrax-backup /etc/cron.d/attrax-backup
# 手工本地归档入口：/usr/local/sbin/attrax-backup。
