# Vision AI provider (currently using mimoTalk)
VISION_PROVIDER=mimo
MIMOTALK_API_KEY=your_mimotalk_api_key
MIMOTALK_BASE_URL=https://token-plan-sgp.xiaomimimo.com/anthropic/v1
MIMOTALK_MODEL=mimo-v2.5

# ModelScope API embedding
MODELSCOPE_API_KEY=your_modelscope_api_key

DAILY_FREE_SCAN_LIMIT=3

DEMO_MODE=false

# RAG Service backend URL (default: port 8001)
RAG_SERVICE_URL=http://localhost:8001

# CORS: comma-separated allowed origins for the RAG service.
# Required for cross-origin browser calls if frontend and RAG are on different
# domains. If unset, RAG middleware allows no origins (CORS will reject all
# cross-origin browser requests, but the in-server proxy still works).
# Production value (2026-06-21): https://twinbuddy.xyz
RAG_ALLOWED_ORIGINS=https://yourdomain.com
