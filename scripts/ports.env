# Attrax 端口单一来源
#
# 这是 nginx upstream 与 ecosystem.config.cjs 共享的端口真理源。改端口只改这里，
# 不要直接改 nginx vhost 或 ecosystem.config.cjs：
#
#   1. nginx vhost 由 docs/infra/nginx-attrax-vhost-prod.conf.template + 这个文件
#      通过 scripts/render-nginx-vhost.sh 渲染得到，端口通过 __NEXTJS_PORT__ 占位符注入。
#   2. ecosystem.config.cjs 通过 require('./ports.env.cjs') 读取（见脚本顶部）。
#   3. apply-deploy.sh 在每次 deploy 时校验 nginx 真实生效的 upstream 端口是否
#      与这里一致；不一致直接 abort（fail-closed，不让漂移活到下一次 reload）。
#
# 加新端口请同步更新 docs/infra/PORTS.md（端口分配表）。
NEXTJS_PORT=3000
RAG_PORT=8001
PORTFOLIO_PORT=3002
